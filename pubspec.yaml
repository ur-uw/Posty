name: posty
description: An instagram posting app helper

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons:
  firebase_core: ^1.3.0
  # firebase_analytics: ^7.1.1 no nullsafe... skip it for now
  firebase_auth: ^1.4.1
  cloud_firestore: ^1.0.7

  # google_sign_in:
  # apple_sign_in:
  get: ^4.1.4
  get_storage: ^2.0.2
  
  crypto: ^3.0.1
  http: ^0.13.3
  csv: ^5.0.0


dependency_overrides:
 # analyzer: 1.4.0
 # intl: 0.17.0

dev_dependencies:
  

flutter:
  uses-material-design: true
  assets:
    - assets/images/default.png
    - assets/images/defaultDark.png
