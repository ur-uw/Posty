name: posty
version: 1.0.0+1
publish_to: none
description: A new Flutter project.
environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  csv: ^5.0.0
  cupertino_icons: ^1.0.2
  get: 4.1.4
  http: ^0.13.3
  wizard_widgets:
    path: packages/wizard_widgets

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
